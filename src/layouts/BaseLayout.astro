---
// src/layouts/BaseLayout.astro
import Logo from '../components/Logo.astro';
// Import direct du CSS - IMPORTANT pour le build
import '../styles/global.css';

export interface Props {
  title?: string;
  description?: string;
  image?: string;
  canonical?: string;
}

const { 
  title = "Mon Site Astro",
  description = "Description par défaut de mon site",
  image = "/favicon.svg",
  canonical 
} = Astro.props;

const canonicalURL = canonical ? new URL(canonical, Astro.site) : new URL(Astro.url.pathname, Astro.site);
---

<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    
    <!-- SEO Meta Tags -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={new URL(image, Astro.url.origin)} />
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={new URL(image, Astro.url.origin)} />
    
    <!-- Préchargement des polices critiques -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  </head>
  
  <body>
    <!-- Header avec logo -->
    <header class="site-header">
      <div class="navbar">
        <Logo 
          href="/" 
          size="md" 
          variant="light" 
          interactive={true}
          class="navbar-logo"
        />
        
        <!-- Navigation sera ajoutée ici -->
        <div class="navbar-nav">
          <a href="/" class="nav-link">Accueil</a>
          <a href="/design-system" class="nav-link">Design System</a>
        </div>
      </div>
    </header>
    
    <!-- Contenu principal - COMMENTÉ POUR FOCUS SUR HEADER
    <main class="site-main">
      <slot />
    </main>
    -->
    
    <!-- Footer avec logo - COMMENTÉ POUR FOCUS SUR HEADER
    <footer class="site-footer">
      <div class="footer-content">
        <div class="footer-brand">
          <Logo 
            size="lg" 
            variant="light" 
            class="footer-logo"
          />
          <p class="footer-description">
            Terre Digitale - Solutions numériques innovantes
          </p>
        </div>
        
        <div class="footer-links">
          <div class="footer-section">
            <h4>Navigation</h4>
            <ul>
              <li><a href="/">Accueil</a></li>
              <li><a href="/design-system">Design System</a></li>
            </ul>
          </div>
        </div>
      </div>
      
      <div class="footer-bottom">
        <p>&copy; 2024 Terre Digitale. Tous droits réservés.</p>
      </div>
    </footer>
    -->
    
    <!-- Scripts optionnels -->
    <script>
      console.log('🚀 Site chargé avec le système de design tokens');
    </script>
  </body>
</html>

<style>
  /* Styles spécifiques au layout avec padding --space-32 */
  .site-header {
    background-color: var(--color-primary-light);
    position: sticky;
    top: 0;
    z-index: var(--z-navbar);
    padding: var(--space-md);
  }
  
  .navbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    max-width: 100%;
    margin: 0;
  }
  
  .navbar-nav {
    display: flex;
    gap: var(--space-sm);
    align-items: center;
  }
  
  .nav-link {
    color: var(--color-neutral-lighter);
    text-decoration: none;
    font-weight: var(--font-weight-medium);
    transition: var(--transition-colors);
    padding: var(--space-sm) var(--space-md);
    text-transform: uppercase;
  }
  
  .nav-link:hover {
    color: var(--color-highlight);
  }
  
  /* FOOTER STYLES - COMMENTÉ POUR FOCUS SUR HEADER
  .site-main {
    min-height: auto;
    padding: var(--space-32);
  }
  */
  
  /* FOOTER STYLES - COMMENTÉ POUR FOCUS SUR HEADER
  .site-footer {
    background-color: var(--color-primary-dark);
    color: var(--color-neutral-lighter);
    padding: var(--space-32);
    margin-top: auto;
    border-top: 1px solid var(--color-primary-lighter);
  }
  
  .footer-content {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: var(--space-xl);
    margin-bottom: var(--space-xl);
  }
  
  .footer-brand {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }
  
  .footer-description {
    color: var(--color-neutral-dark);
    margin: 0;
    max-width: 300px;
  }
  
  .footer-links {
    display: flex;
    gap: var(--space-xl);
  }
  
  .footer-section h4 {
    color: var(--color-neutral-lighter);
    margin-bottom: var(--space-md);
    font-size: var(--font-size-sm);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  
  .footer-section ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  
  .footer-section li {
    margin-bottom: var(--space-sm);
  }
  
  .footer-section a {
    color: var(--color-neutral-dark);
    text-decoration: none;
    font-size: var(--font-size-sm);
    transition: var(--transition-colors);
  }
  
  .footer-section a:hover {
    color: var(--color-highlight);
  }
  
  .footer-bottom {
    padding-top: var(--space-lg);
    border-top: 1px solid var(--color-primary-lighter);
    text-align: center;
  }
  
  .footer-bottom p {
    color: var(--color-neutral-dark);
    font-size: var(--font-size-sm);
    margin: 0;
  }
  */
  
  /* Assurer que le body prend toute la hauteur */
  html, body {
    height: 100%;
  }
  
  body {
    display: flex;
    flex-direction: column;
  }
  
  /* Responsive */
  @media (max-width: 768px) {
    .navbar {
      flex-direction: column;
      gap: var(--space-md);
      text-align: center;
    }
    
    .navbar-nav {
      flex-direction: column;
      gap: var(--space-sm);
    }
    
    /* FOOTER RESPONSIVE - COMMENTÉ
    .footer-content {
      grid-template-columns: 1fr;
      text-align: center;
    }
    
    .footer-links {
      justify-content: center;
    }
    */
    
    /* Réduire le padding sur mobile */
    .site-header,
    .site-main {
      padding: var(--space-lg);
    }
  }
  
  @media (max-width: 640px) {
    .navbar-nav {
      gap: var(--space-xs);
    }
    
    .nav-link {
      padding: var(--space-xs) var(--space-sm);
      font-size: var(--font-size-sm);
    }
    
    /* Padding encore plus réduit sur très petits écrans */
    .site-header,
    .site-main {
      padding: var(--space-md);
    }
  }
</style>