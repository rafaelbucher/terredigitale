---
// src/pages/index.astro
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Accueil - Terre Digitale"
  description="Nous croyons en un numérique qui allie performance, responsabilité et accessibilité. Avec Terre Digitale, transformez vos produits en leviers de changement positif."
>
  <div class="hero-container">
    <div class="hero-content">
      <p class="hero-statement">
        Nous croyons en un numérique qui allie performance, responsabilité et accessibilité. Avec Terre Digitale, transformez vos produits en leviers de changement positif.
      </p>
    </div>
  </div>
</BaseLayout>

<style>
  /* Background global de la page */
  :global(body) {
    background-color: var(--color-primary) !important;
  }

  :global(.site-header) {
    background-color: var(--color-primary) !important;
    border-bottom: none;
  }

  /* Hero container prend toute la hauteur disponible */
  .hero-container {
    min-height: calc(100vh - 120px); /* Ajuster selon la hauteur du header */
    display: flex;
    align-items: flex-end;
    justify-content: start;
    width: 100%;
    padding: 0 calc(var(--space-2xl) + var(--space-24));
  }

  .hero-content {
    width: 100%;
    max-width: 800px;
    margin-bottom: var(--space-4xl);
  }

  .hero-statement {
    font-family: var(--font-family-primary);
    font-size: var(--font-size-3xl);
    font-style: italic;
    font-weight: var(--font-weight-lighter);
    line-height: var(--line-height-tight);
    letter-spacing: .015em;
    color: var(--color-neutral-lighter);
    margin: 0;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero-container {
      min-height: calc(100vh - 100px);
    }

    .hero-content {
      margin-bottom: var(--space-3xl);
    }

    .hero-statement {
      font-size: var(--font-size-3xl);
      line-height: var(--line-height-normal);
    }
  }

  @media (max-width: 640px) {

    .hero-content {
      margin-bottom: var(--space-2xl);
    }

    .hero-statement {
      font-size: var(--font-size-xl);
    }
  }

  /* Très petits écrans */
  @media (max-width: 375px) {
    .hero-statement {
      font-size: var(--font-size-3xl);
    }
  }
</style>