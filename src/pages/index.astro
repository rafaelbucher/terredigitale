---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Accueil - Terre Digitale"
  description="Pour un numérique inclusif et durable"
>
  <section class="section-hero section section--full">
    <p class="hero-quote">« L’accessibilité numérique ne devrait pas être considérée comme une option, mais comme un droit fondamental. »</p>
    <p class="hero-author">Tim Berners-Lee</p>
  </section>

  <section class="section-intro section section--full">
    <h2 class="baseline-title">Des produits digitaux performants, responsables <br>et inclusifs.</h2>
    <p class="baseline-text">Terre Digitale transforme vos sites et applis en moteurs de croissance et d’impact positif. Des expériences numériques alliant innovation, accessibilité et responsabilité pour un impact durable.</p>
  </section>

  <section class="pillars-section section section--full">
    <div class="pillars-container">
      <div class="pillars-grid">
        <div class="pillar-card">
          <h3 class="pillar-name">Inclusion et ouverture</h3>
          <p class="pillar-description">
            Rendez vos services accessibles et élargissez votre audience.
          </p>
        </div>
        
        <div class="pillar-card">
          <h3 class="pillar-name">Sobriété et performance</h3>
          <p class="pillar-description">
            Réduisez l'empreinte carbone de vos sites tout en améliorant leur rapidité.
          </p>
        </div>
        
        <div class="pillar-card">
          <h3 class="pillar-name">Durabilité et agilité</h3>
          <p class="pillar-description">
            Construisez des bases solides, faciles à maintenir et à faire évoluer.
          </p>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* ================================
   * HERO SECTION - MOBILE FIRST
   * ================================ */

  .section-hero {
    height: 100svh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    background: var(--color-primary);
    padding-left: var(--space-lg);
    padding-right: var(--space-lg);
  }

  .section-intro {
    height: 100svh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    background-color: var(--color-secondary);
    padding-left: var(--space-lg);
    padding-right: var(--space-lg);
  }

  .section-intro h2,
  .section-intro p {
    white-space: pre-wrap;
    word-spacing: normal;
  }

  .baseline-title {    
    font-family: var(--font-family-primary);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-medium);
    line-height: var(--line-height-normal);
    letter-spacing: 0.05em;
    text-align: left;
    color: var(--color-neutral-lighter);
    margin: 0 0 var(--space-xl) 0;
  }

  .baseline-text {
    font-family: var(--font-family-primary);
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-lighter);
    color: var(--color-neutral-lighter);
    margin: 0;
    letter-spacing: 0.05em;
    text-align: left;
  }

  .hero-quote {
    font-family: var(--font-family-primary);
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-medium);
    line-height: var(--line-height-normal);
    letter-spacing: 0.05em;
    text-align: left;
    color: var(--color-neutral-lighter);
    margin: 0 0 var(--space-md) 0;
  }

  .hero-author {
    font-family: var(--font-family-primary);
    font-size: var(--font-size-md);
    font-weight: var(--font-weight-lighter);
    line-height: var(--line-height-normal);
    letter-spacing: 0.05em;
    text-align: left;
    color: var(--color-neutral-darker);
    margin: 0 0 var(--space-xl) 0;
  }

  /* ================================
   * PILLARS SECTION - MOBILE STACK
   * ================================ */

  .pillars-section {
    background-color: var(--color-secondary);
  }

  .pillars-container {
    width: 100%;
  }

  .pillars-grid {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
    align-items: stretch;
  }

  .pillar-card {
    padding: var(--space-lg);
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    text-align: left;
    transition: var(--transition-transform);
    box-shadow: var(--shadow-card);
    background-color: var(--color-primary-dark);
    border-radius: 12px;
    border: 1px solid var(--color-primary-lighter);
  }

  .pillar-card:active {
    transform: translateY(2px);
  }

  .pillar-name {
    font-family: var(--font-family-primary);
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-bold);
    color: var(--color-neutral-lighter);
    margin-bottom: var(--space-md);
    line-height: var(--line-height-tight);
  }

  .pillar-description {
    font-family: var(--font-family-primary);
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-regular);
    line-height: var(--line-height-relaxed);
    color: var(--color-neutral-light);
    margin: 0;
  }

  /* ================================
   * TRÈS PETITS ÉCRANS (320px)
   * ================================ */

  @media (max-width: 320px) {
    .hero-container {
      height: calc(100svh - 70px);
      padding-top: var(--space-md);
    }

    .hero-title {
      font-size: var(--font-size-lg);
      line-height: var(--line-height-normal);
    }

    .hero-content {
      margin-bottom: var(--space-lg);
    }

    .pillars-grid {
      gap: var(--space-md);
    }

    .pillar-card {
      padding: var(--space-md);
    }

    .pillar-name {
      font-size: var(--font-size-md);
    }

    .pillar-description {
      font-size: var(--font-size-sm);
    }
  }

  /* ================================
   * VIEWPORT COURT (hauteur réduite)
   * ================================ */

  @media (max-height: 600px) {
    .hero-container {
      min-height: calc(100vh - 60px);
      padding-top: var(--space-sm);
    }

    .hero-content {
      margin-bottom: var(--space-md);
    }

    .hero-title {
      font-size: var(--font-size-lg);
    }
  }
</style>